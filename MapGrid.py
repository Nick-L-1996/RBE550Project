class MapGrid:
    def __init(self, GazeboMap, cellSize):
        pass
    #takes in Gazebo world
    #reads the dimensions of the map
    #Creates Map Nodes in a grid taking into account the map size
    #and desired cell size
    #Scans map at each cell location and identifies the terrain
    #sets terrain as part of grid cell

    #fill class with functions needed for implementation TBD